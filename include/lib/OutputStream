#pragma once

#include <utils>

namespace EXOS::Utils {
  class _OutputStream {
  public:
    virtual void write(const uint8_t byte) = 0;
    virtual void write(Pointer<const void> buf, size_t n) = 0;
  };

  using OutputStream = _OutputStream*;
}